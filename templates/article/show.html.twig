{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}


    

<div class="block-complet">
    <div class="container block-article">
        <div class="titre-categorie">
            <h1>{{ article.name }}</h1>
            <p>catégorie : {{ article.categorie }}</p>
        </div>
        <div class="hr">
            <hr>
        </div>
        <div class="article">
            <div class="block-text">
                {{ article.texte }}
            </div>
                <hr>
            <div class="block-image">
                <img src="{{ article.image }}" alt="">
                <p>Publié par {{ article.auteur }}</p>
                <p>Edité le {{ article.createdAt ? article.createdAt|date('d-m-Y') : '' }}</p>
            </div>
        </div>
    </div>
    </div>
    

    {# <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ article.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ article.description }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ article.texte }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ article.createdAt ? article.createdAt|date('d-m-Y') : '' }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ article.categorie }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ article.auteur }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ article.image }}</td>
            </tr>
        </tbody>
    </table> #}

    {# <a href="{{ path('article_index') }}">back to list</a>

    <a href="{{ path('app_article_edit', {'id': article.id}) }}">edit</a>

    {{ include('article/_delete_form.html.twig') }} #}
{% endblock %}
