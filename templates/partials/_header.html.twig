

{# <nav>
	<div class="image_nav">
		<img src="{{asset('image/Blog-logo-couleur.png')}}" alt="">
	</div>
	<div class="liste">
		<ul class='liste_gauche'>
			<li>
				<a href="{{ path('home.index') }}">Accueil</a>
			</li>
			<li>
				<a href="{{ path('article_index') }}">Blog</a>
			</li>
			
		</ul>
		<ul class='liste_droite'>
			{% if app.user %}
			<li class='liste_connected'>
				<a href="{{ path('app_user') }}"><img src="{{asset('image/icone-profil.png')}}" alt=""></a>
				<p>{{ app.user.Name }}</p>
			</li>
			<li>
				<a href="{{ path('app_logout') }}">Se déconnecter</a>
			</li>
			
			
			{% else %}
			<li>
				<a href="{{ path('app_register') }}">Inscription</a>
			</li>
			<li>
				<a href="{{ path('app_login') }}">Connexion</a>
			</li>
			{% endif %}
			
			
		</ul>
	</div>
</nav> #}


<div class="nav">
  <input type="checkbox" id="nav-check">
  <div class="nav-header">
    <div class="nav-title">
      <img src="{{asset('image/Blog-logo-couleur.png')}}" alt="">
    </div>
  </div>
  <div class="nav-btn">
    <label for="nav-check">
      <span></span>
      <span></span>
      <span></span>
    </label>
  </div>
  
  <div class="nav-links">
    <a href="{{ path('home.index') }}" target="">Accueil</a>
    <a href="{{ path('article_index') }}" target="">Blog</a>
	{% if app.user %}
		
				<a href="{{ path('app_user_index') }}">vous êtes connecté en tant que {{ app.user.Name }}</a>
				
		
			
				<a href="{{ path('app_logout') }}" target=''>Se déconnecter</a>
			
			
			
			{% else %}
			
				<a href="{{ path('app_register') }}" target=''>Inscription</a>
			
			
				<a href="{{ path('app_login') }}" target=''>Connexion</a>
			
			{% endif %}
    
  </div>
</div>